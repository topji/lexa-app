/**
 * WebSocket connection constants
 */
export declare const WS_CONSTANTS: {
    /** Maximum time to wait for connection in milliseconds */
    readonly CONNECT_TIMEOUT: 5000;
    /** Ping interval in milliseconds */
    readonly PING_INTERVAL: 30000;
    /** Time to wait for pong response in milliseconds */
    readonly PONG_TIMEOUT: 5000;
    /** Maximum reconnection attempts */
    readonly MAX_RECONNECTS: 5;
    /** Base delay between reconnection attempts in milliseconds */
    readonly RECONNECT_DELAY: 1000;
    /** Maximum delay between reconnection attempts in milliseconds */
    readonly MAX_RECONNECT_INTERVAL: 10000;
};
/**
 * Regular expressions for validation
 */
export declare const VALIDATION_REGEX: {
    /** Matches valid feed IDs (0x followed by 64 hex characters) */
    readonly FEED_ID: RegExp;
    /** Matches valid schema versions (0x0002-0x0009, 0x000a, 0x000d) */
    readonly SCHEMA_VERSION: RegExp;
};
export declare const DEFAULT_TIMEOUT = 30000;
export declare const DEFAULT_RETRY_DELAY = 1000;
export declare const DEFAULT_RETRY_ATTEMPTS = 1;
export declare const X_CLL_AVAILABLE_ORIGINS_HEADER = "X-Cll-Available-Origins";
export declare const X_CLL_ORIGIN_HEADER = "X-Cll-Origin";
